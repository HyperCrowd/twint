import { resolve } from 'path'

export const databasePath = resolve(__dirname, '../')

export const jsonPath = resolve(__dirname, '../captures')

export const reportPath = resolve(__dirname, '../reports')